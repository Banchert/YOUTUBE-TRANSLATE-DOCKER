# YouTube Video Translator - UNLIMITED Configuration
# ไฟล์นี้ใช้สำหรับการตั้งค่าแบบไม่จำกัดความยาว

# =============================================================================
# 🚀 การตั้งค่าความยาวไม่จำกัด
# =============================================================================

# Processing Configuration - UNLIMITED
MAX_VIDEO_DURATION=0          # 0 = ไม่จำกัดความยาววิดีโอ
MAX_TEXT_LENGTH=0             # 0 = ไม่จำกัดความยาวข้อความ
MAX_FILE_SIZE=2000            # 2GB
MAX_CHUNK_SIZE=10000          # 10,000 ตัวอักษรต่อ chunk
MAX_BATCH_SIZE=500            # 500 ข้อความต่อ batch

# Performance Configuration
CONCURRENT_TASKS=10           # 10 งานพร้อมกัน
TASK_TIMEOUT=28800            # 8 ชั่วโมง
REQUEST_TIMEOUT=60            # 60 วินาที
MAX_RETRIES=5                 # ลองใหม่ 5 ครั้ง

# LibreTranslate Configuration - UNLIMITED
LT_CHAR_LIMIT=0               # ไม่จำกัดความยาว
LT_REQ_LIMIT=1000             # 1,000 request ต่อนาที
LT_BATCH_LIMIT=200            # 200 ข้อความต่อ batch
LT_THREADS=16                 # 16 threads

# TTS Configuration - UNLIMITED
TTS_MAX_TEXT_LENGTH=0         # ไม่จำกัดความยาว
TTS_CHUNK_SIZE=5000           # 5,000 ตัวอักษรต่อ chunk
TTS_SAMPLE_RATE=22050         # คุณภาพเสียงสูง

# Whisper Configuration - UNLIMITED
WHISPER_MODEL=large           # ใช้ model ใหญ่ที่สุด
WHISPER_DEVICE=cpu            # หรือ cuda ถ้ามี GPU
WHISPER_TIMEOUT=3600          # 1 ชั่วโมง

# Memory Configuration
REDIS_MAX_MEMORY=2gb          # 2GB สำหรับ Redis
POSTGRES_MAX_CONNECTIONS=100  # 100 connections

# =============================================================================
# 📝 วิธีการใช้งาน
# =============================================================================

# 1. คัดลอกไฟล์นี้เป็น .env:
# cp unlimited-config.env .env

# 2. รีสตาร์ทระบบ:
# docker-compose down
# docker-compose up -d

# 3. ตรวจสอบการตั้งค่า:
# curl http://localhost:5000/languages

# =============================================================================
# ⚠️ ข้อควรระวัง
# =============================================================================

# - ใช้ RAM และ CPU มากขึ้น
# - ไฟล์ผลลัพธ์จะมีขนาดใหญ่
# - ใช้เวลาประมวลผลนานขึ้น
# - ควรมีพื้นที่เก็บข้อมูลเพียงพอ

# =============================================================================
# 🎯 ตัวอย่างการใช้งาน
# =============================================================================

# แปลวิดีโอยาว 2 ชั่วโมง:
# - ข้อความ: ~50,000 ตัวอักษร
# - เวลา: 30-60 นาที
# - ไฟล์ผลลัพธ์: 500MB-1GB

# แปลหนังสือทั้งเล่ม:
# - ข้อความ: ~100,000 ตัวอักษร
# - เวลา: 1-2 ชั่วโมง
# - ไฟล์ผลลัพธ์: 1-2GB 